package Week_8_Lab;

import java.awt.*;

public class Square extends Shape 
{
	
	public Square()
	{
		//Set Square's Color: Blue
		SetColor(Color.BLUE);
		
		//Set Square's size
		SetWidth(Shape.BASE_SIZE);
		SetHeight(Shape.BASE_SIZE);
	}
}
